# This is an example configuration for shepherd. Do not modify. Instead make a
# copy and name it config.yml.
command: record
parameters:
  no_calib: false
  force: false
  ldo_voltage: 2.0 # TODO: does not exist in this form anymore
  load: artificial # TODO: does not exist in this form anymore
  mode: harvesting
  output: /var/shepherd/recordings
verbose: 2
