# This is an example configuration for shepherd. Do not modify. Instead make a
# copy and name it config.yml.
command: record
parameters:
  mode: harvesting
  duration: 10
  no_calib: true
  output_path: /var/shepherd/recordings/rec_test.h5
  force_overwrite: true
verbose: 2

